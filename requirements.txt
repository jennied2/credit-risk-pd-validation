# --- core (required; the repo runs on these alone) ---
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7

# --- canonical libraries the fallbacks stand in for (recommended) ---
xgboost>=2.0
shap>=0.44
optbinning>=0.19
fairlearn>=0.10
statsmodels>=0.14
seaborn>=0.12

# --- notebook ---
jupyter>=1.0
nbformat>=5.9
